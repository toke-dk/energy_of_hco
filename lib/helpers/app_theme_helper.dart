import 'package:flutter/material.dart';

TextTheme getAppTextTheme(context) {
  return Theme.of(context).textTheme;
}

ColorScheme getAppColorScheme(context) {
  return Theme.of(context).colorScheme;
}
